# Dependencias de desarrollo y CI (backend).
# Incluye requirements.txt (producción) + herramientas de calidad.
#
# Uso:
#   pip install -r requirements-dev.txt
#
# Firma XAdES-BES (VeriFactu) y cliente HTTP async: necesarios para tests y runtime;
# se repiten aquí para visibilidad en pipelines que solo lean este archivo.
-r requirements.txt

signxml>=4.0.0
httpx>=0.27.0

pytest-cov>=5.0.0
